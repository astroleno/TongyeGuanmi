# R5 Production Parity Repair Contract Diff

Status: regression closure implemented on the branch; final candidate-v2 qualification is in progress. The prior HITL rejection remains authoritative until the corrected exact source passes the full gate.

Date: 2026-07-12. Branch: `codex/react-refactor-r5-parity-cutover`. Repair base: `59065730712c6d9718928fd25cba23e33455395e`.

## Release Identity Boundary

The immutable tags `react-refactor-r5-candidate`, `react-refactor-r5-candidate-v2`, `react-refactor-r5-candidate-v3`, and `react-refactor-r5-parity-repair-candidate` predate this HITL regression closure. They remain audit records and must not be moved, reused, or described as containing the corrected runtime.

The rejected review head is `2501704d63dbd7c150861d21a31c2d39525c23e5`. After the complete gate passes, this closure is authorized to create the new annotated tag `react-refactor-r5-parity-repair-candidate-v2`; it never authorizes moving an old tag, merging or deploying `main`, creating `react-refactor-r5-cutover`, or starting R6 cleanup.

## Preserved Architecture

- 18 holds, 17 segments, canonical scene order, scene ids, hashes, and public copy are unchanged.
- Director, SegmentPlayer, Stage, LayerWindow, HandleRegistry, production lazy loading, harness lazy loading, and the crawlable static shell remain the owning architecture.
- No legacy runtime, second default entry, fake scene, old query fallback, or global scroll runtime was reintroduced.
- The repair adds lifecycle ownership inside existing modules; it does not split or replace the architecture.

## Reproduction, Root Cause, And Minimum Responsibility

| ID | Deterministic reproduction before repair | Root cause | Minimum responsibility files | Corrected contract |
|---|---|---|---|---|
| R1 | Sample AOD → Method through progress `0..0.333`; Method is covered by paper-colored surfaces instead of showing through authored WebM alpha. | The AOD article, sticky field, reveal surface, and paper backing stayed opaque while the entire layer remained at full opacity. | `app/src/scenes/aod-animation/progress.ts`, `app/src/transitions/aod-method-top/index.ts`, `app/src/styles.css` | The first third clears every AOD paper backing while preserving layer opacity and the single Method copy cue at `0.8`; compositing comes from the asset alpha. |
| R2 | Time Crane → Contact; R5 runs about 4.2 seconds and its renderer/media timing disagrees with the requested figure window. | Duration was duplicated across manifest and scene behavior instead of using one authority. | `app/src/story/timings.ts`, `app/src/story/manifest.ts`, `app/src/scenes/crane-animation/index.tsx`, `app/src/transitions/crane-contact/index.ts` | `CRANE_CONTACT_DURATION_MS = 3000` owns manifest, transition, renderer, media windows, copy cue, and assertions. |
| R3 | Enter Hero ↔ Pattern and sample Pattern revisions while visible; they stop outside the committed Pattern hold. | Continuous motion was equated with `role === current`. | `app/src/stage/scene-motion.ts`, `app/src/scenes/pattern/index.tsx`, `app/src/transitions/hero-pattern/index.ts` | A run-scoped lease keeps the 24fps Pattern renderer live whenever Pattern visibly participates and releases idempotently. |
| R4 | Let Pattern collapse reach its staged pause, then continue into Pattern → Star Map; rotation freezes after structural progress stops. | `freezeMotion: true` tied authored rotation to deterministic structural progress. | `app/src/scenes/pattern/index.tsx`, `app/src/transitions/pattern-star-map/index.ts` | Structural collapse remains deterministic while wall-clock rotation stays live through the pause and ink stage. |
| R5 | Sample Star Map noise revisions during forward and reverse Pattern ↔ Star Map; incoming/outgoing Star Map remains static. | Star Map used the same committed-role-only ownership as Pattern. | `app/src/stage/scene-motion.ts`, `app/src/scenes/star-map/index.tsx`, `app/src/transitions/pattern-star-map/index.ts` | A visibility-gated lease keeps the existing 12fps Perlin renderer live only while Star Map is visible. |
| R6 | Alternate Star Map ↔ AOD repeatedly; later runs retain the horizontal clip but lose the rendered ink body. | A scene-owned canvas was reused after its WebGL context had been explicitly lost. | `app/src/transitions/star-map-aod/index.ts`, `app/src/transitions/shared/sceneInk.ts`, `app/src/vendor/ink-scene-transition.js` | Every run owns a fresh Stage canvas/context; generation guards cover loss, disposal, image/frame callbacks, and stale runs. |
| R7 | Cold-load Hero and inspect the two loader phrases; the rejected build used a CSS inset wipe instead of the accepted Ink reveal and could start Hero more than once. | Production restored loader timing without porting the app-owned text-mask/Ink renderer lifecycle. | `app/src/production/loader-ink-sequence.ts`, `app/src/production/loader-ink-reveal.ts`, `app/src/production/StoryLoader.tsx`, `app/src/production/StoryApp.tsx`, `app/src/scenes/hero/motion.ts`, `app/src/styles.css` | Cold Hero owns one lazy WebGL text-mask/FBM/wet-edge/droplet controller through both phrases, then starts the 2.7s Hero intro once. Font delay, context loss, reduced motion, timeout, and no-JS use bounded accessible fallback and release all RAF/GL/listener resources. |
| R8 | Inspect the production header on Hero and Pattern; the replacement pill shell lacks the progressive sibling blur and remains in the wrong lifecycle. | Navigation markup and visibility were owned by an unrelated replacement header. | `app/src/production/StoryNav.tsx`, `app/src/production/StoryNav.css`, `app/src/production/StoryApp.tsx` | `nav.site-nav.has-scroll-edge-blur + div.scroll-edge-blur` is an exact sibling pair with seven blur layers plus tint; both share committed-hold visibility and hidden/inert tab semantics. |
| R9 | Wheel/touch/key inside Method before its physical edge; Director charge can advance before all copy is consumed. | Reading handoff and Director each owned a different accumulator/threshold lifecycle. | `app/src/stage/reading.ts`, `app/src/production/reading-handoff.ts`, `app/src/production/gesture-intent-gate.ts`, `app/src/production/input-controller.ts` | One physical gesture gate owns cadence, direction, idle reset, and the 10svh commitment. Reading consumes content pixels first and releases one normalized residual intent only at the physical edge. |
| R10 | At a reading edge or Contact hold, send ordinary slow deltas through 9.9svh and then cross 10svh; the rejected path could decay or require rapid swipes. | Event-rate-dependent Director charge was stacked after reading/hold accumulation. | `app/src/production/gesture-intent-gate.ts`, `app/src/production/input-controller.ts`, `app/src/runtime/charge.ts` | 9.9svh cannot fire. Crossing 10svh emits exactly one intent independent of ordinary event cadence; Director does not apply a second distance band. Idle, reversal, entry, scene, seek, resize, orientation, and dynamic viewport changes reset ownership. |
| R11 | Reach Contact or disable JavaScript; no semantic company/footer/filing block is present. | Interactive Contact and generated static shell had separate incomplete footer content. | `app/src/content/site-meta.ts`, `app/src/components/SiteFooter.tsx`, `app/src/scenes/contact/index.tsx`, `app/build/static-shell.ts` | Both paths render the same company, tagline, and MIIT filing link from one source; compact-height CSS keeps the Contact footer in the viewport. |
| R12 | Inspect built HTML; favicon is an inline placeholder `T` rather than the canonical legacy asset. | R5 embedded a data URL instead of emitting the tracked source icon. | `app/index.html`, `app/scripts/verify-release-build.mjs` | HTML references `assets/favicon.svg`; release verification compares emitted and source bytes and rejects data URLs. |
| R13 | Reverse from Contact while Crane preparation/playback/endpoint reconstruction fails; Hero can become current or flash as fallback. | Every failure reused the boot-only global `fallbackLayerWindow()`. | `app/src/runtime/recovery.ts`, `app/src/runtime/director.machine.ts`, `app/src/runtime/director.actor.ts` | Boot recovery alone may use Hero. Segment recovery preserves Contact, reconstructs the directional Crane endpoint, stays interactive on endpoint failure, and is cancelled by newer interaction generations. |
| R14 | Reverse sequentially into a long reading hold; it opens at the top or is reset there by a later render. | Entry direction/source was not committed in Director context and positioning ran outside entry identity. | `app/src/runtime/director.machine.ts`, `app/src/production/StoryApp.tsx`, `app/src/stage/reading.ts` | Each committed entry has one token: forward sequential/menu/hash opens top, reverse sequential opens exact bottom in a layout effect, and ordinary renders cannot reposition it. |
| R15 | Reverse Figure2 stage 2 → stage 1 and sample presented frames; the rejected path flashed, seek-stormed, or jumped to an endpoint. | Per-frame reverse seeks and numeric progress armed depth ownership before reverse media/mask presentation was ready. | `app/src/media/directional-media-controller.ts`, `app/src/scenes/figure2-animation/index.tsx`, `app/src/transitions/figure2-distance-expand/index.ts`, `app/src/transitions/shared/depthThresholdMask.ts` | Dedicated forward/reverse media surfaces are prepared per leg and played natively after the first direction-correct frame is presented. Depth ownership commits only after its mask is ready; failures stay segment-local. |
| R16 | Inspect computed font stacks and built assets; Inter is first, `Tongye Title` is named but not loaded, and static/enhanced tokens drift. | Global font declarations were replaced without carrying the font asset or canonical fallback stacks. | `app/index.html`, `app/src/styles.css`, `app/scripts/verify-release-build.mjs` | The canonical TTF is preloaded/emitted, `@font-face` binds weight 400, SF/PingFang sans and Songti/Source Han serif tokens are shared, and synthetic weights are disabled. |
| R17 | Sample Figure2 → Proof opening; the retained near arch is included in/covered by the depth grade plane. | The foreground plane was not isolated above the Figure2-specific ink surface. | `app/src/stage/RetainedFigure2Arch.tsx`, `app/src/stage/Stage.tsx`, `app/src/transitions/figure2-distance-expand/index.ts`, `app/src/styles.css` | One retained DOM arch stays outside every depth-mask target and above the Figure2 ink canvas until Proof → Brand owns its exit. |
| R18 | Reverse within the same TTG/PH staged run, including before visiting the receiver; delayed or stale readiness could lock, flash, or overwrite the active leg. | Leg readiness was inferred from mutable rendered progress and surfaces/drivers were reconfigured during render rather than owned by a fixed run generation. | `app/src/media/directional-media-controller.ts`, `app/src/transitions/shared/stagedMediaHandoff.ts`, `app/src/scenes/ph-animation/index.tsx`, `app/src/scenes/ttg-animation/index.tsx` | Each staged leg prepares a fixed direction-specific presented frame before its authored clock. Same-run reversals, rejection, interruption, and stale callbacks are generation guarded; inactive directions remain parked and local failure cannot time out the whole segment. |
| R19 | Finish or reverse TTG around its first pause; the active surface can show the stale forward terminal, standing frame, or endpoint-only reverse. | Surface activation and media-time writes raced the first decoded target frame. | `app/src/scenes/ttg-animation/index.tsx`, `app/src/media/directional-media-controller.ts`, `app/src/transitions/shared/stagedMediaHandoff.ts` | Source terminal stays frozen during the 600ms dissolve. Forward/reverse surfaces activate atomically only after the fixed first frame is presented, native playback traverses intermediate frames, and reverse completion restores the prepared forward-start surface. |
| R20 | Inspect production ink during radial/horizontal/depth handoffs; a full-screen dark body grades the whole scene. | Boundary geometry and a cinematic cover alpha were one inseparable preset. | `app/src/transitions/shared/sceneInk.ts`, `app/src/transitions/shared/ink.ts`, `app/src/vendor/ink-scene-transition.js`, `app/src/harness/r3/PilotHarness.tsx` | Production defaults to `edge-only` with zero scene-wide cover. `dark` remains a named harness-only comparison using identical boundary geometry and ownership. |
| R21 | Slow a horizontal Generic Ink handoff against high-contrast source/receiver colors; a faceted or uncovered ownership seam can appear in either direction. | The 32-point polygon was visibly coarse and the procedural shader front could move away from the DOM ownership contour. | `app/src/transitions/shared/horizontalInkContour.ts`, `app/src/transitions/shared/inkField.ts`, `app/src/transitions/shared/ink.ts`, `app/src/transitions/shared/sceneInk.ts`, `app/src/transitions/star-map-aod/index.ts`, `app/src/vendor/ink-scene-transition.js` | One immutable per-run 128-sample contour drives complementary source/receiver polygons and the exact 1×128 texture coordinates. A narrow fully opaque shader core is centered on that contour; FBM/tendrils decorate without moving ownership. Renderer loss is a typed segment failure, production cover alpha remains zero, and no SVG/snapshot/scene texture is introduced. |
| R22 | Traverse TTG → Lab and PH → Education in both directions; a second Generic Ink appears inside an already established media chapter. | Chapter-internal media handoff reused the cross-chapter Ink transition instead of owning only the two media states. | `app/src/transitions/shared/stagedMediaHandoff.ts`, `app/src/transitions/ttg-lab/index.ts`, `app/src/transitions/ph-education/index.ts` | Each internal handoff keeps two live staged media surfaces and performs one 600ms complementary opacity dissolve. It creates no Ink canvas, polygon, mask, transform, blur, scale, or particle layer. |

## Lifecycle Invariants

- Motion leases, transition canvases, WebGL contexts, video seeks/frame callbacks, reading accumulators, and recovery work are keyed to a run or interaction generation.
- Settle, abort, seek, recovery, StrictMode remount, scene unmount, and duplicate disposal are idempotent.
- Hidden/reduced-motion Pattern and Star Map loops stop; authored motion is not permanently frozen to satisfy performance.
- TTG's progress-zero hold uses the canonical poster with both video surfaces parked metadata-only. During playback only the requested direction is decoded; required-direction readiness promotes it before use. Surface state and every shared driver are disposed with their owning media element.
- A horizontal Ink timeline owns one immutable 128-sample contour revision for its lifetime; a fresh invocation owns a fresh revision. The one-row texture is uploaded once per revision, never per progress frame, and renderer invalidation cannot leave a bare polygon running.
- Generic Ink retains exactly two live canonical scenes plus one effect-only canvas. TTG/PH internal dissolves are separately revertible and never acquire Generic Ink ownership.
- Copy remains owned by the existing single copy cue; the repair does not add duplicate scene copy.

## Acceptance Boundary

Historical green results at `14743aa` and the later HITL-rejected head are not qualification evidence for this closure. Final acceptance must be regenerated from one clean source and must include root verification, both browser matrices, the focused direction/media/Ink paths, frame pacing, process memory/disposal, exact identity-bound build, and same-port rollback.

Only after all gates pass may `react-refactor-r5-parity-repair-candidate-v2` be created. Work then stops for HITL. Candidate creation is not permission to merge, deploy, create a cutover tag, or begin R6.
