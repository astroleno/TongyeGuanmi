{
  "name": "tongye-guanmi-site",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@8.15.1",
  "type": "module",
  "scripts": {
    "dev": "pnpm -C app dev",
    "dev:web": "pnpm run dev",
    "build": "pnpm -C app build",
    "preview": "pnpm -C app preview",
    "test": "pnpm -C app test",
    "lint": "pnpm -C app lint",
    "typecheck": "pnpm -C app typecheck",
    "test:browser": "pnpm -C app test:e2e",
    "test:release": "pnpm -C app test:release",
    "verify:all": "pnpm run lint && pnpm run typecheck && pnpm run test && pnpm run build",
    "ci": "pnpm run verify:all && pnpm run test:release",
    "deploy:prepare": "pnpm -C app run release:prepare",
    "deploy:finalize": "pnpm -C app run release:finalize",
    "deploy:build": "pnpm run deploy:prepare && pnpm -C app run evidence:memory:release && pnpm run deploy:finalize",
    "legacy:build": "node scripts/build-index.mjs",
    "legacy:dev": "node scripts/serve-static-site.mjs",
    "legacy:verify:copy": "node scripts/check-copy-alignment.mjs",
    "legacy:verify:ink-modules": "node scripts/check-ink-modules.mjs",
    "legacy:verify:scroll-modules": "node scripts/check-scroll-modules.mjs",
    "legacy:verify:section-transitions": "node scripts/check-section-transition-contract.mjs",
    "legacy:verify:transition-runtime": "node scripts/check-transition-runtime.mjs",
    "legacy:verify:homepage-transitions": "node scripts/check-homepage-transition-integration.mjs",
    "legacy:verify:handoff-ownership": "node scripts/check-handoff-ownership.mjs",
    "legacy:verify:all": "pnpm run legacy:build && pnpm run legacy:verify:copy && pnpm run legacy:verify:ink-modules && pnpm run legacy:verify:scroll-modules && pnpm run legacy:verify:section-transitions && pnpm run legacy:verify:transition-runtime && pnpm run legacy:verify:homepage-transitions && pnpm run legacy:verify:handoff-ownership",
    "legacy:ci": "pnpm run legacy:verify:all"
  }
}
